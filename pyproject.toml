[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecac-login"
version = "0.1.2"
description = "Login automatizado no portal eCAC via Patchright com suporte a certificado digital"
requires-python = ">=3.10"
dependencies = [
    "patchright",
    "python-dotenv",
    "ResolvedorCaptcha @ git+https://github.com/tecnologia-save/ResolvedorCaptcha-Python.git@4c1c8d49e0fe4eaa2b24321540b7a184951fda29",
]

[dependency-groups]
# Ferramentas de desenvolvimento — NAO sao dependencias de runtime da lib.
# Instalacao: pip install --group dev   (requer pip >= 25.1)
dev = ["pytest>=8,<10"]

[tool.setuptools.packages.find]
where = ["."]
include = ["ecac_login*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]